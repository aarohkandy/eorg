{
  "name": "gmail-unified-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Gmail Unified extension + IMAP backend",
  "scripts": {
    "setup:backend": "npm --prefix apps/backend install",
    "dev:backend": "npm --prefix apps/backend run dev",
    "start:backend": "npm --prefix apps/backend run start",
    "test:imap": "npm --prefix apps/backend run test:imap"
  }
}
